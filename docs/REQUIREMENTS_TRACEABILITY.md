# Requirements Traceability

> Current-state matrix, reviewed 2026-07-12. Verified remediation release SHA `457c7c2` passed 276 Node tests and 13 Python security tests; CI, Agentathon Preflight, and GitHub Pages are green; and the authenticated production workflow was verified at <https://parallax42-agent-v2.vercel.app>. Detailed findings and acceptance gates are in the [deep code review](DEEP_CODE_REVIEW.md); the selected future hosting path is in the [Azure migration plan](AZURE_MIGRATION_PLAN.md).

| G42 Requirement | Current Evidence | Status | Exceed Strategy |
| --- | --- | --- | --- |
| Live production deployment | Vercel browser app and Node APIs at <https://parallax42-agent-v2.vercel.app>, isolated Railway PostgreSQL/Qdrant, and GitHub Actions Docker `/run` contract proof. SHA `457c7c2` has green CI/Preflight/Pages checks and an authenticated production workflow verification. The FastAPI wrapper has no claimed public Railway deployment. | Verified working demo | Keep release checks green without presenting health metadata as an assurance result. |
| Cloud-agnostic or Azure-compatible | A repository-grounded Azure reference architecture, phased migration, rollback path, and acceptance gates are documented. | Planned | Execute [the Azure migration plan](AZURE_MIGRATION_PLAN.md) with IaC and parity tests; the current deployment remains Vercel/Railway. |
| Secure API / sovereign LLM integration | A named server-side Compass gateway client supplies smart intake, Node advisory specialists, and semantic embeddings; provider and vector-store keys do not reach the browser. Deterministic fallback remains available. | Strong demo / partial enterprise | Close the gateway, tenant-isolation, runtime-selection, and public diagnostic findings in the deep review before enterprise use. |
| Structured data processing/reporting | Agent outputs JSON decisions, controls, evidence IDs, trace; `npm run capture:evidence` exports JSON artifacts. | Strong | Add CSV export and signed audit pack endpoint. |
| Conversational enterprise workflow | Chat-first UI plus `/api/conversation` NLP case builder extracts fields, carries stable active-question IDs/fields, asks missing-context questions, and executes the governed council workflow. | Strong | Keep adding regression tests for high-value terse answers instead of training by one-off prompt patches. |
| Explainability and audit traceability | Trace/evidence/readiness/digest fields plus durable actor-scoped PostgreSQL audit chains exist. Audit writes are append-only through the application; detailed reads are role-gated/tenant-filtered/private; `/api/logs` is removed; hosted writes fail closed without Postgres. This is not immutable/WORM storage or proof of `enterpriseReady`. | Strong demo / partial enterprise | Add WORM range export, restore proof, versioned migrations, database policy, and critical business/audit transaction coupling. |
| Exception handling and escalation | Gaps include severity and action; the exact decision contract is `ready`, `conditionally_ready`, or `not_ready`. | Strong | Add role owner mapping and SLA/due-date fields. |
| RBAC/authentication | Route policy middleware and isolated demo sessions are enforced. Audit reads are role/scoped. Entra-compatible JWT code exists, but Microsoft Entra tenant configuration, memberships, app roles, RLS, and enterprise SSO proof are pending. | Strong demo / partial enterprise | Configure issuer, audience, tenant, JWKS, memberships/roles, and database policy before enterprise authorization claims. |
| Responsible AI controls | Human review, Node-only policy authority, non-proof question/mention states, source-level contradictions, nonterminal conditional status, and authoritative case-version continuation are covered by the 276-test Node release suite and 13 Python security tests; the authenticated production workflow is verified. | Strong verified demo / partial enterprise | Keep adversarial/parity/lifecycle tests required in CI; add claim-level grounding and immutable policy hashes. |
| Performance benchmarks | Local benchmark endpoint, benchmark script, Parallax42 20/20 golden eval evidence, and hardware/import regression tests. | Partial | Add load, latency, fallback-rate, and upload/OCR benchmarks. |
| Prior deployments/references | Live Parallax42 deployment and generated health evidence artifacts. | Partial | Add deployment screenshots, endpoint proofs, and reference note. |
| Technical architecture | `docs/TECHNICAL_ARCHITECTURE.md` plus visual diagram in `docs/AGENTATHON_SYSTEM_ARCHITECTURE.md`. | Strong | Add data-flow threat model later. |
| Integration capabilities | Integration matrix, Parallax42 ingestion API design, and sample payloads for Coupa, ServiceNow, SharePoint, and Dynamics. | Strong | Add live replay screenshots and integration contract tests. |
| Video demonstration | Script planned. | Gap | Record "Watch the Agent Work" using live demo route. |
| Agent orchestration / CrewAI | The hosted product uses Node advisory specialists through Compass plus deterministic fallback. Python CrewAI adapters, agent/task YAML, and dry-run checks exist but live Python CrewAI is optional and inactive. | Partial | Make runtime telemetry distinguish requested, attempted, executed, and fallback modes; add parity/eval gates before enabling Python CrewAI. |

## Positioning

At verified implementation SHA `457c7c2`, the submission can claim the online-first product demo, root `/run` wrapper, named-Compass intake/Node specialists, isolated Qdrant retrieval, Node-only policy authority, authoritative multi-turn state, and durable scoped PostgreSQL audit chains that are append-only through the application. It must not claim a public FastAPI evaluator, enterprise SSO/membership/RLS, immutable/WORM audit retention, `enterpriseReady` audit, arbitrary scanned-PDF OCR, Qdrant in every evaluator path, active Python CrewAI, or enterprise-grade decision assurance. Residual export/restore, identity, RLS, retention, admission, and business/audit coupling gates remain explicit.
