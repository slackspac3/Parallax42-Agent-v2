# Logs

Runtime audit logs are written to `logs/agent_audit.jsonl` locally and to the
configured `AGENT_AUDIT_DIR` in deployed environments. The repository includes
sample JSONL traces for submission review only; generated runtime logs remain
gitignored.
